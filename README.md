# tech_solution

# Для запуска использовать docker compose build затем docker compose up
# При запуске бд в ней создаются таблицы без данных. 
# Для более простого переноса из одного яп в другой было принято решение не ииспользовать орм sqlalchemy а написать с исользованием  #драйвера asyncpg

# Проблемы которые были встречены в задании:
# Работа с токенами, для реализации был выбран jwt токен, однако после удаления из спецификации, код для безопасности был удален. 
# Еще одна проблема была с асинхронным подключением к бд, решением стало запуск приложения со слипом. Так как проблема была в том что бд  # не успевалась создаться и приложение не могло работать с ниим 